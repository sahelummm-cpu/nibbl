import React from "react";
import { createRoot } from "react-dom/client";
import Nibbl from "../Nibbl.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Nibbl />
  </React.StrictMode>
);
